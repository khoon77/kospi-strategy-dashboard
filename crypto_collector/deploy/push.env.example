# Copy to push.env (chmod 600) and fill in a GitHub Personal Access Token
# scoped to "Contents: Read and write" on just this repo (fine-grained PAT).
# Never commit push.env -- it's already covered by the repo-root .gitignore
# rule for crypto_collector/deploy/push.env, added when this file was added.
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
